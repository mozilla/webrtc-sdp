language: rust
dist: trusty
cache: cargo
sudo: true
os:
  - linux
# Taken out temporarily because it's to slow
#  - osx

rust:
  - stable

matrix:
  allow_failures:
    - rust: nightly-2017-06-19

before_install:
  - sudo apt-get update

addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - cmake
      - gcc
      - binutils-dev

# Add clippy
before_script:
  - export PATH=$PATH:~/.cargo/bin
  - |
    if [[ "$TRAVIS_RUST_VERSION" == "nightly-2017-06-19" ]]; then
      cargo install --force clippy;
    fi

script:
  - cargo build --verbose --all
  - |
    if [[ "$TRAVIS_RUST_VERSION" == "nightly-2017-06-19" &&
      -f ~/.cargo/bin/cargo-clippy ]]; then
      cargo clippy;
    fi
  - cargo test --verbose --all

after_success:
  - |
      if [[ "$TRAVIS_OS_NAME" == "linux" && "$TRAVIS_RUST_VERSION" == "stable" ]]; then
        wget https://github.com/SimonKagstrom/kcov/archive/master.tar.gz &&
        tar xzf master.tar.gz &&
        cd kcov-master &&
        mkdir build &&
        cd build &&
        cmake .. &&
        make &&
        sudo make install &&
        cd ../.. &&
        rm -rf kcov-master &&
        for file in target/debug/rsdparsa-*[^\.d]; do mkdir -p "target/cov/$(basename $file)"; kcov --exclude-pattern=/.cargo,/usr/lib --verify "target/cov/$(basename $file)" "$file"; done &&
        bash <(curl -s https://codecov.io/bash) &&
        echo "Uploaded code coverage"
      fi
