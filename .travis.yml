language: rust

dist: trusty

rust:
  - nightly-2017-06-19

os:
  - linux

sudo: false

cache:
  cargo: true

matrix:
  allow_failures:
    - rust: nightly

# Add clippy
before_script:
  - |
      if [[ "$TRAVIS_RUST_VERSION" == "nightly-2017-06-19" ]]; then
        ( ( cargo install clippy && export CLIPPY=true ) || export CLIPPY=false );
      fi
  - export PATH=$PATH:~/.cargo/bin

script:
  - cargo build --verbose --all
  - cargo test --verbose --all
  - echo "Rust version $TRAVIS_RUST_VERSION"
  - echo "Test X$CLIPPY"
  - |
      if [[ "$TRAVIS_RUST_VERSION" == "nightly-2017-06-19" && $CLIPPY ]]; then
        cargo clippy;
      fi
